# Three-obligation closure, 2026-09-07

The exact browser-generated evidence was committed by CI in 7433b1cfec6ad75e5a9c5ef321f3a0586aed3cec, from run https://github.com/supercubegame/flappycat/actions/runs/34090559688 at source 254a54d1f187a0a1a295ff357c43d44841b372ae. Final PR checks must validate these committed files, not just the temporary CI workspace.

## Screenshots
Three actual rendered canvas PNG images are embedded unchanged in the existing SVG paths. The initial element screenshot measured 480x641 because CSS bounding boxes and the internal 480x640 canvas are different measurement surfaces. Capture now exports native canvas PNG pixels in Chromium; no rescaling, redrawing or generated illustration. Full provenance and SHA-256 hashes are in docs/shots/provenance.json. Sizes: ready 23872, play 15732, dead 30873 bytes. The application source was not modified.

## Audio
The actual Space -> flap -> blip path was rendered using a real OfflineAudioContext. Sound-on produced 3086 nonzero samples out of 22050, peak 0.05967969447374344. Muted mode created zero contexts and zero nodes. The disconnected negative control still started one oscillator but its 22050 output samples were all zero. Existing realtime node-start tests remain intact. This demonstrates actual graph output, NOT sound from a human user's physical speaker. Speaker audibility and perceived quality remain explicitly unmeasurable by this CI and need human acceptance.

## Obligations
All three original entries, reasons and original deadlines are preserved in docs/COMPLETED-OBLIGATIONS.json. Screenshot substitution and first scheduled-run observation are fulfilled; the sound item is split into verified PCM output and the documented human speaker boundary. The pending ledger is empty only after those dispositions were recorded and verified. Permanent fast checks guard the exact three closure identities, screenshot hashes/dimensions/unique payloads and audio evidence. New pending entries report all invalid/overdue/completion-marker problems, with malformed-date, duplicate-ID, unreadable-evidence and empty-ledger controls. Deadlines were not extended.

The recorded first scheduled run is 2026-09-06T07:55:08.428Z, run 34020380803. It establishes that scheduling ran, not that the product gate was green that day. The original heartbeat-freshness and manual-heartbeat separation tests remain unchanged.
